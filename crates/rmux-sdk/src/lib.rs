#![deny(missing_docs)]
#![forbid(unsafe_code)]

//! Public daemon-backed RMUX SDK scaffolding.
//!
//! This crate is intentionally compile-only in the initial v1 workspace
//! scaffold. It exposes no public default_value API before protocol contracts
//! are owned by `rmux-proto`.

mod scaffold;
